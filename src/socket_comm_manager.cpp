#include <stdio.h>
#include <stdlib.h>
#include "socket_comm_manager.h"

using namespace std;

int socket_comm_manager::connect( char* address, int port_number ) {
    return 0;
}
int socket_comm_manager::disconnect() {
    return 0;
}
int socket_comm_manager::send_message( char* message, socket_comm_callback* callback ) {
    return 0;
}
